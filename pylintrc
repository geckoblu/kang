[MASTER]

# Add files or directories to the blacklist. They should be base names, not
# paths.
ignore=mainWindowBA.py,aboutDialogBA.py,regexLibraryWindowBA.py,newUserDialogBA.py,reportBugDialogBA.py,regexReferenceWindowBA.py,preferencesDialogBA.py,importURLDialogBA.py


[MESSAGES CONTROL]

# Enable the message, report, category or checker with the given id(s). You can
# either give multiple identifier separated by comma (,) or put this option
# multiple time. See also the "--disable" option for examples.
#enable=

# Disable the message, report, category or checker with the given id(s). You
# can either give multiple identifiers separated by comma (,) or put this
# option multiple times (only on the command line, not in the configuration
# file where it should appear only once).You can also use "--disable=all" to
# disable everything first and then reenable specific checks. For example, if
# you want to run only the similarities checker, you can use "--disable=all
# --enable=similarities". If you want to run only the classes checker, but have
# no Warning level messages displayed, use"--disable=all --enable=classes
# --disable=W"
#disable=
disable=missing-docstring,old-style-class,too-few-public-methods,locally-disabled,no-self-use,unused-argument,bad-builtin,superfluous-parens,no-init,duplicate-code,bare-except,broad-except


[BASIC]

# Regular expression which should only match correct module names
module-rgx=([a-z_][a-zA-Z0-9]*)$

# Regular expression which should only match correct module level names
const-rgx=(([A-Z_][A-Z0-9_]*)|(__.*__)|(__[a-z][a-zA-Z0-9_]*))$

# Regular expression which should only match correct class names
class-rgx=[A-Z_][a-zA-Z0-9]+$

# Regular expression which should only match correct function names
function-rgx=[a-z_][a-zA-Z0-9]{2,35}$

# Regular expression which should only match correct method names
method-rgx=(([a-z_][a-zA-Z0-9]{2,35})|(test_[a-zA-Z0-9_]{2,35})|(__init__)|(__str__)|(__eq__)|(__repr__)|(exec_))$

# Regular expression which should only match correct instance attribute names
attr-rgx=[a-z_][a-zA-Z0-9]{1,35}$

# Regular expression which should only match correct argument names
argument-rgx=[a-z_][a-zA-Z0-9]{1,35}$

# Regular expression which should only match correct variable names
variable-rgx=([a-z_][a-zA-Z0-9]{0,35}|(exc_type)|(exc_value)|(exc_traceback)|(xdg_config_home)|(config_dir))$

# Regular expression which should only match correct attribute names in class
# bodies
class-attribute-rgx=([A-Za-z_][A-Za-z0-9_]{2,35}|(__.*__))$

# Good variable names which should always be accepted, separated by a comma
good-names=i,j,k,ex,Run,_,x,y


[FORMAT]

# Maximum number of characters on a single line.
max-line-length=160


[DESIGN]

# Maximum number of arguments for function / method
max-args=10

# Maximum number of statements in function / method body
max-statements=70

# Maximum number of attributes for a class (see R0902).
max-attributes=30

# Maximum number of public methods for a class (see R0904).
max-public-methods=99

# Maximum number of locals for function / method body
max-locals=20



